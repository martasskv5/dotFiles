#!/usr/bin/env bash
# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  ##
# Legacy Waybar script retained for compatibility.
# Noctalia manages its own bar layout, so this is now a no-op.

notify-send -u low "Waybar Layout" "Noctalia manages bar layout in its own settings"
exit 0

